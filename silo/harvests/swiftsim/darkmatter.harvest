#!/bin/bash
# Author: Shree Raj Shrestha
# Date: 02/12/2025
set -ueox pipefail
source /soil/home
source /soil/rake "${1}" "${2}"
source 
mkdir -p "${THIS_HARVEST}"
cd "${THIS_HARVEST}"
ls -la "${W}"

#echo "${1}" "${2}"
# python3 ${FARM}/${SIM}/plotProjection.py 31

