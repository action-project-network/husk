STEPS=(
    "./configure --prefix=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/${WHICH_SEED} \
        --with-zlib=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/zlib/ \
        --enable-shared=no \
        --enable-build-mode=production \
        --enable-parallel"
    "make -j12"
    "make check"
    "make install"
    "make check-install"
)
NEEDS=(openmpi_5.0.6_default zlib_1.3.1_default)