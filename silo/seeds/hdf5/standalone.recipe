STEPS=(
    "./configure --prefix=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/${WHICH_SEED} \
        --with-zlib=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/zlib/ \
        --enable-shared=no \
        --enable-build-mode=production"
    "make -j12"
    "make check"
    "make install"
    "make check-install"
)
NEEDS=(zlib_1.3.1_default)