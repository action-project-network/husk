STEPS=(
    "FITSDIR=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/cfitsio/lib FITSINC=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/cfitsio/include C_SHARED=0 C_FITS=1 ./configure --auto=c"
    "make -j12 c-all"
    "make c-test"
    "mkdir -p ${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/${WHICH_SEED}"
    "cp -R * ${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/${WHICH_SEED}"
)
NEEDS=(cfitsio_4.5.0_default)