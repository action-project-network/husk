STEPS=(
    "rm -rf GKlib"
    "ln -s ${SOIL_HOME}/${WHICH_COB}/gklib ./GKlib"
    "make config \
        shared=1 \
        cc=gcc \
        prefix=${SOIL_HOME}/${WHICH_COB}/${WHICH_SEED}"
    "make all"
    "make install"
)
NEEDS=(gklib_latest_default)