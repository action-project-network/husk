STEPS=(
    "rm -rf GKlib"
    "ln -s ${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/gklib ./GKlib"
    "make config \
        cc=gcc \
        prefix=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/${WHICH_SEED}"
    "make -j12 all"
    "make install"
)
NEEDS=(gklib_5.1.1_default)