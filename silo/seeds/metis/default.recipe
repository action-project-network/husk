STEPS=(
    "rm -rf GKlib"
    "ln -s ${SOIL_HOME}/${WHICH_COB}/gklib ./GKlib"
    "make config \
        shared=1 \
        cc=gcc \
        prefix=${SOIL_HOME}/${WHICH_COB}/${WHICH_SEED}"
    "make -j12 all"
    "make install"
)
NEEDS=(gklib_5.1.1_default)