STEPS=(
    "./autogen.sh"
    "./configure \
        --prefix=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/${WHICH_SEED} \
        --with-hdf5=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/hdf5/bin/h5cc \
        --with-fftw=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/fftw \
        --with-metis=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/metis \
        --with-gsl=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/gsl \
        --with-numa=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/numa \
        --with-jemalloc=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/jemalloc \
        --with-grackle=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/grackle \
        --with-chealpix=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/healpix \
        --with-cfitsio=${SOIL_HOME}/${NURSERY_HOME}/${WHICH_POD}/cfitsio"
    "make -j12 all"
    "make install"
)
NEEDS=(
    hdf5_1.14.5_parallel \
    openmpi_5.0.6_default \
    fftw_3.3.10_parallel \
    metis_5.1.1_default \
    gsl_2.8_default \
    numa_2.0.19_default \
    jemalloc_5.3.0_default \
    grackle_latest_default \
    healpix_3.83_default \
    cfitsio_4.5.0_default
)