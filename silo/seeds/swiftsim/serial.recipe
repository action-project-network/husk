STEPS=(
    "./autogen.sh"
    "./configure \
        --prefix=${SOIL_HOME}/${WHICH_COB}/${WHICH_SEED} \
        --with-hdf5=${SOIL_HOME}/${WHICH_COB}/hdf5/bin/h5cc \
        --with-fftw=${SOIL_HOME}/${WHICH_COB}/fftw \
        --with-gsl=${SOIL_HOME}/${WHICH_COB}/gsl \
        --with-metis=${SOIL_HOME}/${WHICH_COB}/metis"
    "make -j12 all"
    "make install"
)
NEEDS=(
    hdf5_latest_serial \
    openmpi_latest_serial \
    fftw_latest_serial \
    metis_latest_serial \
    gsl_latest_serial \
    numa_latest_serial \
    jemalloc_latest_serial
)