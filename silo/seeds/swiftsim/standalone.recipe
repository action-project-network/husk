STEPS=(
    "./autogen.sh"
    "./configure \
        --prefix=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/${WHICH_SEED} \
        --with-hdf5=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/hdf5/bin/h5cc \
        --with-fftw=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/fftw \
        --with-metis=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/metis \
        --with-gsl=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/gsl \
        --with-numa=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/numa \
        --with-jemalloc=${SOIL_HOME}/${COB_HOME}/${WHICH_POD}/jemalloc"
    "make -j12 all"
    "make install"
)
NEEDS=(
    hdf5_1.14.5_standalone \
    fftw_3.3.10_standalone \
    metis_5.1.1_default \
    gsl_2.8_default \
    numa_2.0.19_default \
    jemalloc_5.3.0_default
)