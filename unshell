#!/bin/bash
# Author: Shree Raj Shrestha
# Date: 02/14/2025
set -ueox pipefail

source "${HUSK_HOME}/home"
source "${HUSK_HOME}/rake" "${1}" "${2}"
eval "${4}"
source "${3}"

cd "${5}"
for STEP in "${STEPS[@]}"; 
    do eval "${STEP}" 2>&1 | tee -a "${HUSK_HOME}/.${WHICH_POD}.log"
done
